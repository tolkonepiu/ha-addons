{
  "extends": ["config:base"],
  "dependencyDashboard": false,
  "packageRules": [
    {
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "automerge": true
    }
  ],
  "regexManagers": [
    {
      "description": "Update base image versions",
      "fileMatch": [
        "^(\\w)+\\/build\\.yaml$"
      ],
      "matchStrings": [
        "ghcr.io\\/(?<depName>.+?):(?<currentValue>v?.* ?)"
      ],
      "datasourceTemplate": "docker"
    }
  ]
}
