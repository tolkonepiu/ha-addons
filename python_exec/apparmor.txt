#include <tunables/global>

profile python_exec flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Capabilities
  capability,
  file,
  signal,

  capability setgid,
  capability setuid,

  # S6-Overlay
  /init rix,
  /bin/** ix,
  /usr/bin/** ix,
  /etc/s6/** rix,
  /run/s6/** rwix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /dev/i2c-1 mrwkl,

  # Bashio
  /usr/lib/bashio/** ix,
  /tmp/** rw,

  # Data access
  /data/** rw,
}
